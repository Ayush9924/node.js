console.log(global);
// global is a global object that’s available everywhere in your code — just like window is in a web browser.


console.log("welcone to goa");
globalThis.console.log("welcome");
globalThis.console.log(process);
// curt running process
globalThis.console.log(module);
// A module is simply a file that contains code (variables, functions, classes, etc.) which you can reuse in other files.
// require function for using module. 
// npm install < packages;
